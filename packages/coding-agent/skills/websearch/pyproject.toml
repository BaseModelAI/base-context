[project]
name = "base-context-skill-websearch"
version = "0.1.0"
description = "Web search skill for Base Context (Google search via Serper API)."
requires-python = ">=3.10"
dependencies = ["httpx>=0.27,<1", "base-context-runtime"]

[project.scripts]
websearch = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/websearch"]
